/** Căile modulului social. Vezi `features/events/eventRoutes.ts`. */

/** Favorite + like-uri (trimise / primite). */
export const FAVORITES_PATH = '/favorites';

/** Lista de blocare. */
export const BLOCKLIST_PATH = '/blocklist';
